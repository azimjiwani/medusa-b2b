// next-intl root config file (JS version so it can be loaded at runtime)
// Usato per soddisfare la ricerca automatica del file di configurazione.
// La logica reale di caricamento messaggi è centralizzata in src/i18n/config.ts
module.exports = {
    locales: ['it', 'en'],
    defaultLocale: 'it'
}
